REQUIRED_DEMAND_COLUMNS = {
    "resource_id",
    "district_code",
    "time",
    "demand"
}

REQUIRED_DISTRICT_STOCK_COLUMNS = {
    "district_code",
    "resource_id",
    "quantity"
}

REQUIRED_STATE_STOCK_COLUMNS = {
    "state_code",
    "resource_id",
    "quantity"
}

REQUIRED_NATIONAL_STOCK_COLUMNS = {
    "resource_id",
    "quantity"
}

REQUIRED_BRIDGE_COLUMNS = {
    "state_id",
    "district_id",
    "state_code",
    "district_code"
}
